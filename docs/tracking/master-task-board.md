# Master Task Board

- **Purpose:** Provide the canonical backlog and execution status for project preparation and delivery.
- **Created:** 2026-03-18
- **Last Updated:** 2026-03-23
- **Related Documents:** `priority-roadmap.md`, `post-m3-reactivation-guide.md`, `m3-f3-implementation-handoff.md`, `candidate-a-promotion-readiness.md`, `candidate-a-gap-inventory.md`, `candidate-a-narrow-candidate-comparison.md`, `candidate-b-promotion-readiness.md`, `candidate-b-gap-inventory.md`, `candidate-b-narrow-lane-comparison.md`, `candidate-b-import-migration-approach-decision.md`, `candidate-c-promotion-readiness.md`, `candidate-c-gap-inventory.md`, `environment-follow-up-lane-comparison.md`, `resolved-preview-sub-lane-comparison.md`, `../architecture/request-environment-resolution-summary-contract.md`, `../tasks/task-019-server-backed-pre-import-preview.md`, `../tasks/task-020-candidate-b-gap-inventory-and-lane-selection.md`, `../tasks/task-021-candidate-c-gap-inventory-and-seam-selection.md`, `../tasks/task-022-post-t021-priority-review.md`, `../tasks/task-023-candidate-b-import-migration-approach-decision.md`, `../tasks/task-024-m3-f3-implementation-handoff.md`, `../tasks/task-025-post-m3-f3-closure-priority-review.md`, `../tasks/task-026-m3-f3-validation-environment-blocker-triage.md`, `../tasks/task-027-placeholder-route-mvp.md`, `../tasks/task-028-post-t027-candidate-a-readiness-refresh.md`, `../tasks/task-029-request-environment-selection-and-resolution-plan.md`, `../tasks/task-030-request-environment-selection-and-runtime-resolution.md`, `../tasks/task-031-post-t030-priority-and-candidate-a-refresh.md`, `../tasks/task-032-post-t030-environment-follow-up-lane-comparison.md`, `../tasks/task-033-post-t032-resolved-preview-sub-lane-comparison.md`, `../tasks/task-034-post-t033-resolution-summary-contract.md`, `../tasks/task-035-compact-shell-header-and-material-icon-usability-refresh.md`, `../tasks/task-036-button-badge-and-radius-visual-hierarchy-refinement.md`, `../architecture/client-i18n-foundation.md`, `../tasks/task-037-client-i18n-foundation-and-korean-locale-bootstrap.md`, `../tasks/task-038-environments-and-scripts-route-localization-pass.md`, `../tasks/task-039-workspace-authoring-localization-pass.md`, `../tasks/task-040-workspace-result-panel-localization-pass.md`, `../tasks/task-041-captures-observation-route-localization-pass.md`, `../tasks/task-042-history-observation-route-localization-pass.md`, `../tasks/task-043-shell-density-and-collapsible-navigation-refinement.md`, `../tasks/task-044-single-panel-route-tabs-layout.md`, `../tasks/task-045-mocks-route-localization-pass.md`, `../tasks/task-046-shell-and-management-generated-aria-localization-pass.md`, `../tasks/task-047-route-panel-scroll-containment-follow-up.md`, `../tasks/task-048-observation-route-focused-explorer-overlay-and-density-refinement.md`, `../tasks/task-049-observation-route-card-content-preservation-and-badge-rail-alignment.md`, `../tasks/task-050-workspace-management-focused-overlay-and-content-preservation.md`, `../tasks/task-051-workspace-request-flow-and-canonical-request-tree.md`, `../tasks/task-052-workspace-request-group-creation-and-save-placement-controls.md`, `../prd/overview.md`, `../tasks/task-001-foundation-architecture.md`
- **Update Rule:** Update status, dependencies, notes, and outputs whenever task state changes.

## Status Legend
- `todo`: defined but not started
- `doing`: actively in progress
- `blocked`: cannot proceed due to unresolved dependency or decision
- `done`: completed and handoff-ready

## Canonical Backlog
| ID | Title | Description | Priority | Status | Rationale / Purpose | Dependencies | Deliverables | Recommended Role | Risks | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T001 | Foundation architecture and domain model | Define target module boundaries, core domain entities, persistence candidates, and migration approach from the current monolith. | P0 | done | Unlocks nearly every downstream implementation decision and reduces high-risk rework. | PRD summary alignment | Architecture decision doc, domain model draft, migration outline | Architecture Agent | Premature stack choices, unresolved storage/security trade-offs | Completed through architecture overview, domain model, and migration docs |
| T002 | Delivery documentation and tracking system | Establish PRD summary, roadmap, board, task docs, and collaboration rules. | P0 | done | Enables coordinated execution and clean handoff before feature work. | None | `docs/` structure, AGENTS, workflow guidance | Product / Documentation Agent | Docs can drift if not maintained | Created in this change set |
| T003 | UX information architecture and workspace flows | Define top-level navigation, panel layout, request editing flow, and key user journeys. | P0 | done | Prevents frontend rewrite churn and aligns storage/API decisions with user flow. | T001 | IA doc, screen map, user journey notes | Product + UX / Architecture | Could be blocked by unresolved desktop/web scope | MVP flows documented in IA and workspace-flows docs |
| T004 | Persistence strategy decision | Decide between JSON-file, SQLite, or hybrid persistence for collections, history, environments, and mock rules. | P0 | done | Affects repositories, import/export, backup, and migration complexity. | T001 | Storage decision memo, pros/cons, initial schema approach | Architecture Agent | Rework if chosen too late | Hybrid direction documented; feeds T009 bootstrap |
| T005 | Script execution safety model | Define sandbox runner boundaries, allowed APIs, timeouts, and execution lifecycle. | P0 | done | Current VM model is high risk; must be re-scoped before advanced automation work. | T001 | Execution model design, threat notes, API surface proposal | Architecture + Security-minded Engineer | Underestimated local-security risk | Canonical capability/redaction model now feeds T008/T012 |
| T006 | Frontend stack and application shell decision | Select target frontend framework/tooling and define shell structure for the new workspace UI. | P0 | done | Needed before frontend modularization or editor integration work begins. | T001, T003 | Stack decision doc, shell structure plan | Architecture / Frontend Lead | Tooling churn, overdesign | React/Vite-style SPA shell and lazy editor guidance documented |
| T007 | Shared domain schema and naming conventions | Define canonical models for Request, Collection, Environment, History, MockRule, ScriptTemplate, and execution results. | P0 | done | Ensures cross-team consistency in APIs, storage, and UI. | T001 | Schema doc, glossary, field conventions | Architecture Agent | Naming drift across docs/code | Shared schema and naming docs now act as canonical references |
| T008 | Internal API contract design | Specify service endpoints/events for saved requests, history, mock rules, execution, and captures. | P1 | done | Required before implementation of modular services and UI integration. | T001, T007 | API contract doc, route catalog | Backend / Architecture | Rework if persistence or UX changes | SSE-first contract and stage/event naming documented |
| T009 | Workspace persistence bootstrap | Implement initial storage layer and repository scaffolding. | P1 | done | First concrete code milestone after architecture decisions. | T004, T007 | Storage module scaffold, sample data path | Backend Engineer | Technical debt if done before contracts settle | Storage scaffold and redacted runtime bootstrap are present |
| T010 | Frontend workspace shell implementation plan | Break the UI rewrite into implementation slices with route/state/component boundaries. | P1 | done | Makes large frontend work incremental and reviewable. | T003, T006, T007, T011-T017 | Frontend implementation plan, first-PR handoff | Frontend Lead | Scope explosion | Completed with shell-first slice plan, state boundaries, and first implementation PR scope |
| T011 | Request builder MVP design | Detail params, headers, body, auth, environment resolution, and validation behaviors. | P1 | done | Defines the central user workflow and influences shared schemas. | T003, T007 | Feature spec, edge cases, acceptance criteria | Product + Frontend/Backend | Complex format support may balloon | Request builder MVP scope, tab model, and save/run boundaries documented |
| T012 | Script editor and automation UX spec | Specify Monaco integration level, script types, autocomplete context, and logs panel behavior. | P1 | done | Ensures the “VS Code-like” requirement is translated into implementable scope. | T003, T005, T006, T007 | Script UX spec, capability matrix | Product + Frontend + Architecture | Ambiguous expectation on editor intelligence | Request-bound stage-aware UX, diagnostics, and reusable-script boundaries documented |
| T013 | Mock engine rules spec | Define rule matching, priority, scenario state, delays, and management UI expectations. | P1 | done | Needed to evolve beyond the current single global mock config. | T007, T008 | Mock engine spec | Backend + Product | Matcher scope can become too broad | MVP matcher/priority/diagnostics spec now documented with defer seams for scenarios/scripts |
| T014 | History / inspector behavior spec | Define captured request storage, filters, replay, diff, and unified/unified-not timeline behavior. | P1 | done | Central to the product identity and impacts storage and event design. | T003, T007, T008 | Inspector behavior doc | Product + Backend + Frontend | Data volume and UX complexity | Captures/history split, replay bridge, diff scope, and labeling model documented |
| T015 | Import/export strategy reconciliation | Reconcile the shipped authored-resource transfer baseline with future interoperability and migration deferrals. | P2 | done | Prevents duplicate import/export work and clarifies what remains future-facing after S20-S24. | T004, T007, S20-S24 follow-ups | Import/export strategy note | Architecture / Product | Overstating compatibility or near-term interop | Completed as a documentation task; current support is saved-request/mock-rule authored-resource transfer plus compatibility checks, while cURL/OpenAPI/Postman import stays deferred |
| T016 | Testing and QA strategy | Define validation layers, smoke tests, integration tests, and planning for script/mocking verification. | P1 | done | Prevents fragile refactor and missing regression coverage. | T001, T006, T008 | QA strategy doc, initial test matrix | QA / Senior Engineer | Hard to add late | MVP verification layers, smoke set, and regression guards documented |
| T017 | Developer environment and tooling baseline | Decide linting, formatting, test runner, local data directory conventions, and developer scripts. | P1 | done | Increases productivity once implementation begins. | T006, T009, T016 | Tooling baseline doc, task handoff | Frontend/Backend Lead | Bike-shedding | Completed with tooling baseline, fixture/bootstrap guidance, and CI-friendly script catalog |
| T018 | Delivery milestone plan | Convert the reconciled post-S26 remaining work into phased milestones with readiness gates and review checkpoints. | P1 | done | Keeps execution focused on visual-only follow-up and optional backlog parking now that shell delivery is complete. | T010, T015, Material 3 baseline | Milestone plan | Delivery Agent | Milestone language can accidentally reopen shell scope | Completed as a planning task; `M3-F1`, `M3-F2`, and `M3-F3` are now landed in tracking, while optional backlog remains parked and sandbox-blocked verification is handled through local command handoff rather than as an active blocker |
| T019 | Server-backed pre-import preview for authored-resource bundles | Add a no-write preview step for current workspace bundle import so users can review import consequences before persistence begins. | P2 | done | Promotes the narrowest remaining Candidate A gap without reopening broader resource-tooling scope, M3 work, or shell architecture. | T015, T018, current request/mock authored-resource bundle baseline | Task doc, authoritative preview seam, confirm-before-write flow, tests | Implementation Agent | Preview drift if workspace state changes before confirm, preview scope creep | Landed as a dedicated `import-preview` endpoint plus a workspace preview/confirm/cancel flow that preserves current request/mock bundle scope, `new_identity` semantics, and explorer/mocks refresh behavior after confirm |
| T020 | Candidate B gap inventory and lane selection | Narrow Candidate B from broad migration-engine language into concrete compatibility-gap candidates and identify the strongest future lane without promoting implementation prematurely. | P2 | done | Keeps the next parked workstream grounded in shipped compatibility seams and reduces future promotion ambiguity after `T019`. | T015, T018, T019, current compatibility baseline | Task doc, Candidate B gap inventory, Candidate C readiness note, tracking alignment | Delivery / Tracking Agent | Can still overstate migration urgency or blur parked-versus-active state | Completed as a documentation task; Candidate B remains parked at implementation level, authored-resource import `migration-needed` handling is the strongest future lane, and Candidate C now has a dedicated readiness note rather than only scattered board references |
| T021 | Candidate C gap inventory and seam selection | Narrow Candidate C from broad packaging-polish language into concrete packaging/startup gap candidates and identify whether any verification seam merits later promotion. | P2 | done | Keeps packaging discussion grounded in shipped startup-verification seams and reduces future promotion ambiguity after `T020`. | T018, T020, current packaging/startup verification baseline | Task doc, Candidate C gap inventory, tracking alignment | Delivery / Tracking Agent | Can still mistake environment noise for a real packaging gap or overstate release-readiness needs | Completed as a documentation task; Candidate C remains parked at implementation level, missing-built-shell and sandbox-blocked states are already covered by shipped checks, and no narrow packaging implementation seam is justified yet |
| T022 | Post-T021 priority review | Re-evaluate the post-M3 candidate space after `T021` and record whether any implementation-ready task should be promoted next or whether the repo should remain in explicit hold state. | P2 | done | Prevents momentum-driven promotion after the current Candidate B/C narrowing passes and keeps the roadmap honest after `T021`. | T019, T020, T021, current post-M3 hold-state baseline | Task doc, tracking alignment | Delivery / Tracking Agent | Can still create unnecessary churn if it overstates “no work” rather than documenting the actual triggers | Completed as a planning task; no new implementation task was promoted in that review, `M3-F3` stayed gated, and Candidate 2 / B / C remained parked under the existing reactivation rules |
| T023 | Candidate B import migration approach decision | Narrow the stronger authored-resource import Candidate B lane into one concrete future approach without promoting speculative migration implementation. | P2 | done | Reduces future ambiguity inside Candidate B by choosing one future import-migration framing before any implementation task is considered. | T020, T022, current authored-resource import compatibility baseline | Task doc, Candidate B approach-decision note, tracking alignment | Delivery / Tracking Agent | Can still overfit a future migration path without a real transform contract or legacy sample | Completed as a documentation task; Candidate B remains parked, the stronger future lane now prefers server-owned bundle-level normalization before current import planning, and no implementation task is promoted yet |
| T024 | M3-F3 implementation handoff | Capture the exact pending request-builder/result-panel TSX and CSS patch for `M3-F3` so the active presentation slice can resume without re-auditing scope. | P2 | done | Preserves execution continuity after `gate_clear` by documenting the exact pending wrapper/class patch instead of reopening Material 3 scope discussion. | T018, `M3-F3` gate clear, current request-builder/result-panel DOM | Task doc, `m3-f3-implementation-handoff.md`, tracking alignment | Delivery / Tracking Agent | Can over-document the patch or accidentally imply `M3-F3` is closed before the official gate clears | Completed as a documentation task; the handoff note now records the landed wrapper/CSS patch plus the local verification command set to use whenever sandboxed checks cannot confirm the slice directly |
| T025 | Post-M3-F3 closure priority review | Re-evaluate the parked post-M3 candidate space after `M3-F3` closure and record whether any new implementation task should be promoted next. | P2 | done | Prevents contributors from assuming that closing `M3-F3` should automatically activate the next parked candidate, and preserves one current no-promotion decision after the closure-state change. | T019, T020, T021, T022, T023, T024, T026, current post-M3 closed baseline | Task doc, tracking alignment | Delivery / Tracking Agent | Can still promote weak follow-up work just to avoid a no-promotion conclusion | Completed as a planning task; no new implementation task was promoted after `M3-F3` closure, Candidate A remains the strongest parked area but its remaining `/mocks`-local import lane is still too ambiguous, and Candidate B/C remain parked |
| T026 | M3-F3 validation environment blocker triage | Confirm whether the remaining official `M3-F3` closeout blocker still reflects a repo-side gap or should now be treated as an environment-level validation restriction. | P2 | done | Prevents repeated wrapper/config churn and keeps Candidate C or post-closeout review from being reopened on top of the same already-classified sandbox signal. | T018, T024, S26 wrapper baseline, current closeout evidence | Task doc, tracking alignment, blocker conclusion | Delivery / Tracking Agent | Can overstate an environment-level conclusion from one class of sandbox evidence | Completed as a documentation task; the triage confirmed that bare `esbuild.transform(...)` preflight still fails in this sandbox and that future confirmation should therefore be requested through local commands and expected results instead of another repo-side packaging fix |
| T027 | Placeholder route MVP for Environments, Scripts, and Settings | Replace the remaining top-level placeholder routes with bounded persisted-resource and diagnostics surfaces without widening into selector, linkage, or settings-mutation scope. | P2 | done | Removes the last true shell-level placeholders and makes route inventory match the repo's persisted/runtime capabilities more honestly. | T009, T010, T012, T017, local verification handoff policy | Task doc, environment/script CRUD routes, settings runtime-status route, route UI/tests, tracking alignment | Implementation Agent | Can accidentally widen into request execution binding, shared-script linkage, or settings persistence | Completed as a bounded implementation task; `/environments` and `/scripts` now have workspace-scoped list/detail CRUD surfaces, `/settings` now exposes diagnostics-first runtime status cards, and selector/run/linkage/mutation follow-up work remains explicitly deferred |
| T028 | Post-T027 Candidate A readiness refresh | Refresh Candidate A authored-resource guidance after `T027` made environments and standalone saved scripts real persisted workflow objects. | P2 | done | Prevents future Candidate A decisions from relying on stale placeholder assumptions after route-level environment/script MVP landed. | T019, T025, T027, current Candidate A tracking notes | Task doc, Candidate A doc refresh, tracking alignment | Delivery / Tracking Agent | Can accidentally overstate readiness and trigger premature transfer promotion | Completed as a planning task; Candidate A docs now reflect that environments and standalone saved scripts are real workflow objects, but authored-resource transfer for those resource kinds still remains too broad to promote automatically |
| T029 | Request environment selection and resolution plan | Define the bounded next-step contract for request-level environment selection and server-owned environment resolution after `T027`. | P1 | done | Prevents the next request-builder follow-up from widening into top-bar global state, client-side secret resolution, or broader environment tooling churn. | T011, T027, current execution contract notes | Task doc, environment-selection architecture note, tracking alignment | Architecture + Implementation Agent | Can still leave too much ambiguity around missing references or default-environment behavior if not narrowed explicitly | Completed as a planning task; the next slice is now fixed at request-level selector plus server-owned run-time resolution, while top-bar selector work and broader environment tooling remain deferred |
| T030 | Request environment selection and runtime resolution | Implement the request-level environment selector and server-owned runtime placeholder resolution defined by `T029` without widening into top-bar selector or environment transfer scope. | P1 | done | Closes the main request-builder environment gap left intentionally deferred by `T027` and bounded by `T029`. | T027, T029, current environment CRUD baseline | Task doc, request draft/api/store updates, request header selector, runtime resolution helper, execution/history metadata, validation results | Implementation Agent | Can accidentally widen into full resolved-preview UX, top-bar selector state, or environment import/export work | Completed as a bounded implementation task; request drafts and saved requests now persist `selectedEnvironmentId`, new drafts seed from the current default environment, runtime execution resolves placeholders on the server, missing saved environment references block save/run, execution/history metadata now carries environment id and label, and the `/environments` route now includes contract-linked operator guidance for substitution syntax plus secret/replacement semantics |
| T031 | Post-T030 priority and Candidate A refresh | Re-evaluate the parked next-step space after `T030` and refresh Candidate A guidance now that environments are request-linked run-time actors rather than only route-level workflow objects. | P2 | done | Prevents contributors from assuming that request-level environment implementation should automatically promote environment transfer, top-bar selector work, or another parked candidate. | T028, T030, current Candidate A tracking notes | Task doc, Candidate A doc refresh, tracking alignment | Delivery / Tracking Agent | Can accidentally overstate readiness just because environments now participate in request execution | Completed as a planning task; no new implementation task is promoted after `T030`, environment transfer remains real but broad because it now includes request-reference and run/history coupling questions, and Candidate 2 plus Candidate B/C remain parked |
| T032 | Post-T030 environment follow-up lane comparison | Compare the deferred environment follow-up lanes after `T030` so future work does not reopen top-bar selector, resolved-preview, and transfer scope as one blended theme. | P2 | done | Keeps future environment discussion narrow by identifying one strongest future lane without promoting implementation automatically. | T030, T031, current environment baseline docs | Task doc, environment lane comparison note, tracking alignment | Delivery / Tracking Agent | Can accidentally imply that the strongest future lane is already promoted | Completed as a planning task; richer resolved-preview UX is now the strongest future environment lane if priorities later shift, while top-bar selector work and environment transfer remain parked |
| T033 | Post-T032 resolved-preview sub-lane comparison | Compare the narrower resolved-preview follow-up candidates after `T032` so future work does not reopen pre-run feedback, result/history summaries, and explorer-copy polish as one blended environment-observation theme. | P2 | done | Keeps future environment work narrow by choosing one strongest future sub-lane inside resolved-preview UX without promoting implementation automatically. | T030, T032, current request/result/history environment baseline | Task doc, resolved-preview sub-lane comparison note, tracking alignment | Delivery / Tracking Agent | Can accidentally imply that the strongest sub-lane is already promoted | Completed as a planning task; post-run bounded resolution summary is now the strongest future narrow lane inside resolved-preview UX, while pre-run unresolved-feedback tiering and explorer/readability copy remain parked |
| T034 | Post-T033 resolution summary contract | Define the exact bounded execution/history contract for the post-run environment resolution summary selected by `T033` so a future implementation task stays inside one observation slice. | P2 | done | Keeps future environment-observation work implementation-ready without reopening pre-run validation tiers, full inspectors, or explorer-copy scope. | T030, T033, current execution/history environment metadata baseline | Task doc, architecture contract note, tracking alignment | Architecture Agent | Can still overdesign the future slice if it tries to answer every preview question at once | Completed as a planning task; the future narrow lane is now fixed to one shared `EnvironmentResolutionSummary` contract for active result and persisted history `Execution Info`, while pre-run tiering and explorer/readability copy remain parked |
| T035 | Compact shell header and Material icon usability refresh | Compact the oversized shell header and add a bounded local SVG icon system that improves scanability without changing feature semantics or state ownership. | P1 | done | Improves day-to-day usability now that the shell has real route inventory and request-level environment workflow, while staying inside a client-only visual plus light-UX slice. | T027, T030, current Material 3 baseline | Task doc, compact header/nav refresh, icon primitive, shared-primitives icon support, route/action icon polish, tracking alignment | Implementation Agent | Can accidentally drift into semantics, backend work, or icon-only control patterns | Completed as a client-only Material 3 continuation; the shell header is now route-only and compact, navigation uses local SVG icons instead of monograms, shared primitives accept optional icons, and major route headers/actions keep text-first accessible names while sandbox-blocked UI verification is handled through local rerun handoff |
| T036 | Button, badge, and radius visual hierarchy refinement | Reduce excessive radius usage and clearly separate buttons, badges, and tabs so dense controls read by role instead of repeating the same pill pattern. | P1 | done | Builds directly on T035 by correcting the remaining visual ambiguity in compact controls without changing semantics or ownership. | T035, current Material 3 baseline | Task doc, shape/radius refinement, button-vs-badge contrast, tighter tab geometry, tracking alignment | Implementation Agent | Can accidentally regress control affordance or overshrink dense layouts | Completed as a client-only refinement; buttons now use tighter corners and stronger filled affordance, badges/chips are flatter and lighter, shared tabs use tighter segmented geometry, and sandbox-blocked UI verification is handled through local rerun handoff |
| T037 | Client i18n foundation and Korean locale bootstrap | Establish the client-side internationalization foundation, add Korean locale support, and create the reusable translation and formatting tools that later surface-by-surface localization work can build on. | P1 | done | Creates a reusable localization baseline before broader string migration starts and keeps future Korean translation work from becoming ad hoc per-component churn. | T035, T036, current React shell baseline | Task doc, client i18n architecture note, locale provider, en/ko catalogs, shell and settings proof-of-use, tracking alignment | Implementation Agent | Can widen into full-repo translation churn or accidental contract changes if not kept bounded | Completed as a client-only foundation slice; the app now has locale persistence, shared translation and formatting helpers, English fallback plus Korean catalogs, shell and top-level header localization, and a settings locale switch while sandbox-blocked UI verification is handled through local rerun handoff |
| T038 | Environments and Scripts route localization pass | Extend the T037 i18n foundation across the Environments and Scripts route internals so their CRUD, empty-state, and management-boundary copy can participate in Korean localization without widening into runtime-payload translation. | P1 | done | Creates the first repeatable post-foundation example of localizing a full top-level management surface while preserving backend contracts and English-default test determinism. | T037, current Environments/Scripts route baseline | Task doc, route-level i18n catalog coverage, Korean smoke tests, tracking alignment | Implementation Agent | Can widen into broad full-repo translation churn or server-payload localization if not kept bounded | Completed as a client-only follow-up slice; Environments and Scripts now render client-owned management copy through the shared i18n layer, Korean smoke coverage exists for both routes, and sandbox-blocked UI verification is handled through local rerun handoff |
| T039 | Workspace authoring localization pass | Extend the T037/T038 i18n baseline across the workspace explorer, request tab shell, and request-builder authoring chrome without widening into result-panel or backend-payload translation. | P1 | done | Converts the first major authoring surface after the route-management slices while preserving backend contracts, English-default tests, and bounded observation-surface deferral. | T037, T038, current Workspace authoring baseline | Task doc, workspace route message catalog, Korean smoke coverage, tracking alignment | Implementation Agent | Can widen into observation-panel translation, backend-payload localization, or broad ARIA-contract churn if not kept bounded | Completed as a client-only follow-up slice; workspace authoring chrome, explorer-owned transfer-status messaging, and request-builder authoring copy now render through the shared i18n layer, while observation/result surfaces remain deferred and sandbox-blocked UI verification is handled through local rerun handoff |
| T040 | Workspace result panel localization pass | Extend the T037-T039 i18n baseline across the workspace result panel so right-hand observation chrome can render Korean client-owned copy without widening into captures/history/mocks internals or backend payload translation. | P1 | done | Converts the first bounded workspace observation surface after the authoring slice while preserving backend contracts, English-default tests, and runtime-owned status token boundaries. | T037, T038, T039, current Workspace result-panel baseline | Task doc, workspace result-panel message catalog, Korean smoke coverage, tracking alignment | Implementation Agent | Can widen into broader observation-route translation, backend-payload localization, or runtime token/ARIA-contract churn if not kept bounded | Completed as a client-only follow-up slice; workspace result-panel tabs, observation header copy, response/console/tests/execution-info section copy, and client-owned fallback text now render through the shared i18n layer, while captures/history/mocks observation routes and runtime-owned payload values remain deferred and sandbox-blocked UI verification is handled through local rerun handoff |
| T041 | Captures observation route localization pass | Extend the T037-T040 i18n baseline across the Captures observation route so client-owned route chrome can render Korean without widening into History or Mocks internals, backend payload translation, or runtime status-token localization. | P1 | done | Converts the first top-level observation route after the workspace result-panel slice while preserving backend contracts, English-default tests, and runtime-owned token boundaries. | T037, T038, T039, T040, current Captures route baseline | Task doc, observation-route message catalog coverage, Korean smoke coverage, tracking alignment | Implementation Agent | Can widen into broader observation-route translation, backend-payload localization, or runtime token/ARIA-contract churn if not kept bounded | Completed as a client-only follow-up slice; the Captures route sidebar, filter labels, detail/timeline chrome, replay-bridge actions, and client-owned fallback text now render through the shared i18n layer, while runtime-owned payload values and the History/Mocks observation routes remain deferred and sandbox-blocked UI verification is handled through local rerun handoff |
| T042 | History observation route localization pass | Extend the T037-T041 i18n baseline across the History observation route so client-owned route chrome can render Korean without widening into Mocks internals, backend payload translation, or runtime status-token localization. | P1 | done | Converts the next top-level observation route after the Captures slice while preserving backend contracts, English-default tests, and runtime-owned token boundaries. | T037, T038, T039, T040, T041, current History route baseline | Task doc, observation-route message catalog coverage, Korean smoke coverage, tracking alignment | Implementation Agent | Can widen into broader observation-route translation, backend-payload localization, or runtime token/ARIA-contract churn if not kept bounded | Completed as a client-only follow-up slice; the History route sidebar, filter labels, detail/timeline chrome, replay-bridge actions, result-tab labels, and client-owned fallback text now render through the shared i18n layer, while runtime-owned payload values and the Mocks observation route remain deferred and sandbox-blocked UI verification is handled through local rerun handoff |
| T043 | Shell density and collapsible navigation refinement | Refine the compact shell so the top bar becomes a true 42px row, the navigation rail can collapse to icons only, and shell regions separate through shadow-box depth instead of hard line dividers. | P1 | done | Keeps current shell UX moving forward in a bounded way after T035/T036 without reopening feature semantics, backend contracts, or broader layout redesign. | T035, T036, current shell/i18n baseline | Task doc, compact shell refinements, collapsible nav rail, shell smoke coverage, tracking alignment | Implementation Agent | Can accidentally widen into responsive redesign or break accessible nav labels while collapsing the rail | Completed as a client-only shell refinement; the top bar now fits a 42px density target, runtime status no longer uses the old card wrapper, the navigation rail can collapse to an icon-only mode without losing accessible names, and header/sidebar/main separation now relies on softer shadow-box depth while sandbox-blocked UI verification continues through local rerun handoff |
| T044 | Single-panel route tabs layout | Replace the simultaneous explorer/main/detail route columns with route-local tabs so one panel occupies the full main surface at a time without changing feature semantics. Workspace-oriented routes may bypass the tab strip with a floating explorer dock when that better preserves direct selection flow. | P1 | done | Simplifies scanability inside each route after the shell-density refresh while allowing workspace-oriented routes to recover immediate explorer-to-main/detail feedback without reopening broad shell architecture work. | T043, current route-panel baseline | Task doc, shared route-panel tabs layout, floating-explorer route variant, shell layout CSS update, smoke coverage, tracking alignment | Implementation Agent | Can accidentally break state continuity across tab switches, regress immediate selection visibility, or reintroduce broad multi-column shell churn if the floating dock is not kept bounded | Completed as a client-only layout slice; observation routes keep the route-local tab model, while Workspace/Environments/Scripts now bypass it with a responsive floating explorer dock whose open state is stored per route so explorer selection updates the visible main/detail surfaces immediately and sandbox-blocked UI verification continues through local rerun handoff |
| T045 | Mocks route localization pass | Extend the T037-T044 i18n baseline across the Mocks route internals so client-owned management and observation-adjacent copy can render Korean without widening into backend payload or status-token localization. | P1 | done | Finishes the remaining top-level route-local Korean translation gap while preserving English-default tests, backend contracts, and runtime-owned status-token boundaries. | T037, T038, T039, T040, T041, T042, T044, current Mocks route baseline | Task doc, mocks-route message catalog coverage, Korean smoke coverage, tracking alignment | Implementation Agent | Can widen into backend-payload localization, runtime token translation, or broad ARIA-contract churn if not kept bounded | Completed as a client-only follow-up slice; the Mocks route sidebar/detail/contextual chrome, route-local empty states, authored-rule action labels, detail-tab labels, and helper-generated authored-rule summaries now render through the shared i18n layer, while runtime-owned payload values and status tokens remain unchanged and sandbox-blocked UI verification continues through local rerun handoff |
| T047 | Route panel scroll containment follow-up | Refine the T044 route-panel layout so `.shell-content` stays stretched inside `.shell-body`, floating panels keep an intact 100% containment chain, and long route content scrolls only inside panel surfaces while the explorer behaves as an overlay drawer. | P1 | done | Closes a bounded UX regression risk from T044 by fixing the combined root cause: shell-content stretch failure, floating panel 100% chain collapse, and mixed drawer-overlay/layout-space behavior that let route panels push the page taller than the app shell. | T044, current shell route-panel baseline | Task doc, shell overflow fix, overlay-drawer containment, measured layout regression coverage, tracking alignment | Implementation Agent | Can accidentally reintroduce page-level scrolling, drawer width coupling, mobile viewport quirks, or hidden-content regressions if overflow ownership is inconsistent | Completed as a client-only UX follow-up; `.shell-content` is now a real height-constrained grid container, the floating route-panel chain is re-closed with explicit `min-height: 0`/`height: 100%` containment, the explorer is an overlay drawer that no longer reserves main-surface width, and regression coverage now verifies measured shell/body/panel height relationships instead of only pinning CSS strings |
| T046 | Shell and management generated ARIA localization pass | Extend the T037-T045 i18n baseline across shell route-panel regions plus management/workspace-explorer generated ARIA and action labels without claiming a whole-app ARIA rewrite. | P1 | done | Closes one bounded cross-route accessibility-adjacent localization gap after the route-local Korean copy slices, while keeping request-builder generated ARIA and runtime-owned tokens deferred. | T037, T038, T039, T044, T045, current shell/management/workspace-explorer i18n baseline | Task doc, generated-label catalog additions, Korean smoke coverage refresh, tracking alignment | Implementation Agent | Can overstate broad ARIA completion or accidentally widen into request-builder generated-label churn if not kept narrow | Completed as a client-only follow-up slice; shell route-panel region labels, Environments/Scripts generated list-action/editor labels, and Workspace explorer generated authored-resource action labels now render through the shared i18n layer, while request-builder generated ARIA and runtime-owned status tokens remain unchanged and sandbox-blocked UI verification continues through local rerun handoff |
| T047 | Workspace result panel auto focus and immediate feedback | Refine the workspace result panel so runs surface the most relevant result tab, restore detail-panel visibility in the single-panel layout, and expose immediate diagnostic previews without extra tab switching. | P1 | done | Closes a narrow request-result UX follow-up after `T044` by making post-run feedback more immediately visible without reopening broader layout or observation scope. | T040, T044, current workspace run/result baseline | Task doc, shared result-tab state, auto-focus/detail-panel behavior, bounded preview badges, focused regression coverage | Implementation Agent | Can over-focus empty diagnostics or accidentally reintroduce global result-panel coupling if tab state stops being request-local | Completed as a client-only follow-up slice; workspace runs now auto-select `tests` or `console` from shared per-tab command state, the single-panel workspace layout reselects the detail panel after run completion, and the result summary/header now show immediate outcome badges plus bounded console/test previews. |
| T048 | Observation route focused explorer overlay and density refinement | Refine the `Captures`, `History`, and `Mocks` route explorers so floating navigation behaves as a focused overlay, desktop detail noise drops while the explorer is open, and narrow explorer content still exposes high-signal observation metadata. | P1 | done | Closes the next bounded observation-route UX readability gap after the `T044`/`T047` floating-panel baseline without reopening workspace layout, backend contracts, or deeper observation semantics. | T014, T041, T042, T044, T045, T047, current observation-route explorer baseline | Task doc, focused overlay explorer variant, sticky selected-summary cards, compact observation-row density pass, focused regression coverage, tracking alignment | Implementation Agent | Can accidentally hide too much context, break selection continuity, or regress narrow-screen drawer behavior if overlay/detail visibility rules drift between routes | Completed as a client-only UX follow-up slice; observation routes now use a focused floating explorer variant with a blocking scrim, desktop detail hides while the explorer is open, selecting a capture/history/mock row auto-collapses back into main/detail reading mode, and sticky summary cards keep path/state/time metadata visible without restoring the previous tall list rows. |
| T049 | Observation route card content preservation and badge-rail alignment | Refine the `Captures`, `History`, and `Mocks` observation-route cards so long values remain readable, detail headers keep only high-priority badges on one horizontal rail, and narrow timeline/detail cards stop collapsing into visually compressed nested surfaces. | P1 | done | Closes the next bounded observation-route readability gap after `T048` by preserving internal card content and badge scanning without reopening workspace layout, backend contracts, or broader shell design. | T044, T047, T048, current observation-route detail-card baseline | Task doc, observation detail-header badge-rail override, content-preserving explorer/detail/timeline card pass, focused regression coverage, tracking alignment | Implementation Agent | Can accidentally over-widen cards, regress mobile observation panels, or leak route-specific badge overrides into Workspace if the CSS scope drifts | Completed as a client-only UX follow-up slice; observation-route detail headers now keep their highest-priority badges on one horizontal rail, lower-priority metadata moves into readable meta text/summary cards, explorer rows wrap long values instead of hiding them, and capture/history timeline entries render as one visible card surface rather than a compressed double-frame stack. |
| T050 | Workspace, environments, and scripts focused overlay reuse and content preservation | Extend the `T048`/`T049` focused-overlay and content-preserving card patterns into `Workspace`, `Environments`, and `Scripts` so non-observation routes keep less clutter on screen while still exposing key explorer/detail data. | P1 | done | Closes the next bounded non-observation route readability gap by reusing the proven floating-explorer and badge/card density patterns without reopening settings scope, backend contracts, or broader shell redesign. | T027, T038, T039, T040, T048, T049, current workspace/management route baseline | Task doc, focused overlay reuse, sticky explorer summaries, management/workspace badge-rail/content-preservation pass, focused regression coverage, tracking alignment | Implementation Agent | Can accidentally over-apply observation-route styling, break import/export explorer flows, or regress workspace result readability if the route-scoped CSS hooks drift | Completed as a client-only UX follow-up slice; environments/scripts still reuse the focused floating explorer overlay and route-scoped readability rules, while the Workspace-specific sticky-summary assumption is superseded by `T051` in favor of a canonical persisted request tree plus working-tab separation. |
| T051 | Workspace request flow and canonical request-tree promotion | Redesign Workspace around a canonical persisted request tree and a separate working-tab set, and promote `Collection > Request Group > Request` into the primary saved-resource structure across storage, API, transfer, and UI seams. | P1 | done | Closes the structural workspace drift that remained after `T050`: broken explorer layout, stale tree counts, delete-vs-close ambiguity, and request-placement metadata that was still too folder-like instead of becoming first-class saved-resource structure. | T003, T007, T008, T010, T015, T039, T040, T050, current workspace saved-request baseline | Task doc, request-placement helpers, collection/request-group CRUD and request-tree API, authored-resource bundle v2 normalization, workspace explorer/tree rewrite, detached-draft delete behavior, canonical docs/tracking refresh | Implementation Agent | Can introduce placement migration drift, break saved-request open/save compatibility, or leave docs half-updated if canonical request-group semantics are not carried through every layer | Completed as a cross-layer workspace restructuring slice; collections and request groups are now first-class persisted resources, workspace explorer renders only the canonical saved tree, open tabs remain a separate working set, explicit delete detaches open saved tabs into drafts, and authored-resource bundle transfer plus canonical docs now reflect the new `Collection > Request Group > Request` model. |
| T052 | Workspace request-group creation and save-placement controls | Add the first bounded request-group management UI inside the canonical workspace explorer and expose explicit collection/request-group save-placement controls in the request builder. | P1 | done | Closes the next narrow follow-up after `T051` by letting users create request groups where they save requests and by making canonical placement editable before save, without reopening broad collection CRUD or workspace redesign. | T039, T040, T051, current canonical workspace request-tree baseline | Task doc, request-group create UI, request-builder save-placement controls, draft placement updates, focused regression coverage, tracking alignment | Implementation Agent | Can still leave collection CRUD, rename/delete semantics, or empty-collection placement unclear if this slice expands beyond create-plus-save-placement scope | Completed as a bounded workspace follow-up slice; the explorer can now create request groups inside existing collections, the request builder exposes explicit save collection/request group controls, save payloads carry the selected canonical placement, and later rename/delete or collection CRUD work remains deferred. |
## Current Focus
- **Latest landed implementation:** `T052` is complete. Workspace explorer can now create request groups inside the canonical collection tree, the request builder exposes explicit save collection/request-group controls, save payloads carry the selected placement, and the supporting docs/tests now treat this as the next bounded follow-up after `T051`.
- **Latest landed implementation:** `T049` is complete. Observation-route detail headers now keep only high-priority badges on one horizontal rail, lower-priority metadata is preserved through meta text and summary cards, explorer rows keep long path/body/matcher strings readable, and capture/history timeline entries no longer use the old compressed double-card framing.
- **Previous landed implementation:** `T048` is complete. Observation routes now use a focused floating explorer overlay with a blocking scrim, hide the desktop detail column while the explorer is open, auto-collapse after list selection, and keep high-signal capture/history/mock metadata visible through sticky selected-summary cards plus denser list rows.
- **Previous landed implementation:** `T047` is complete. Workspace runs now promote result-tab focus from shared per-tab command state, automatically return the single-panel workspace layout to the detail panel after run completion, and expose immediate console/test badges plus preview snippets in the result summary.
- **Latest landed implementation:** `T046` is complete. Shell route-panel regions plus Environments, Scripts, and Workspace explorer generated ARIA/action labels now translate through the shared i18n layer, while request-builder generated ARIA labels and runtime-owned status tokens remain unchanged.
- **Latest landed implementation:** `T047` is complete. The shell now keeps `main.shell-content` stretched within `div.shell-body`, the floating route-panel chain keeps a closed 100%/`minmax(0, 1fr)` containment path, the explorer behaves as an overlay drawer instead of reserving layout width, panel-level scroll remains the only intended overflow path, and regression coverage now checks measured shell/body/panel height relationships.
- **Previous landed implementation:** `T046` is complete. Shell route-panel regions plus Environments, Scripts, and Workspace explorer generated ARIA/action labels now translate through the shared i18n layer, while request-builder generated ARIA labels and runtime-owned status tokens remain unchanged.
- **Latest landed implementation:** `T045` is complete. The Mocks route sidebar, authored-rule detail/contextual chrome, route-local empty states, action labels, detail-tab labels, and helper-generated authored-rule summaries now translate through the shared i18n layer, while runtime-owned payload values and status tokens remain unchanged.
- **Latest landed implementation:** `T044` is complete. Workspace-oriented routes plus the active list/detail observation-management routes now use a responsive floating explorer dock so explorer selection updates the visible main/detail surfaces immediately without requiring a second route-panel tab click.
- **Latest landed implementation:** `T043` is complete. The shell top bar now fits a true 42px density target, the runtime-status wrapper has been flattened, navigation can collapse into an icon-only rail while preserving accessible route names, and header/sidebar/main separation now relies more on shadow-box depth than on hard dividers.
- **Latest landed implementation:** `T042` is complete. The History route sidebar, filter labels, detail/timeline chrome, replay-bridge actions, result-tab labels, and client-owned fallback text now translate through the shared i18n layer, while runtime-owned payload values remain unchanged.
- **Latest landed implementation:** `T041` is complete. The Captures route sidebar, filter labels, detail/timeline chrome, replay-bridge actions, and client-owned fallback text now translate through the shared i18n layer, while runtime-owned payload values and other observation routes remain unchanged.
- **Previous landed implementation:** `T040` is complete. Workspace result-panel tabs, observation header copy, response/console/tests/execution-info section copy, and client-owned fallback text now translate through the shared i18n layer, while runtime-owned payload values and broader observation routes remain unchanged.
- **Previous landed implementation:** `T039` is complete. Workspace explorer chrome, request tab shell, request-builder authoring copy, request-bound scripts authoring surface, and explorer-owned authored-resource transfer messaging now translate through the shared i18n layer, while broader observation routes and backend payloads remain unchanged.
- **Previous landed implementation:** `T036` is complete. Buttons now use tighter corners and stronger filled affordance, passive badges/chips are flatter and lighter, shared tabs use tighter segmented geometry, and the previous over-rounded overlap in dense grouped controls has been reduced without changing feature behavior or ownership.
- **Previous landed implementation:** `T035` is complete. The shell header is now a compact route-only breadcrumb bar, navigation uses local SVG icons instead of monograms, shared primitives accept optional icons, and major route headers/actions now use bounded icon-plus-label treatment without changing feature behavior or ownership.
- **Previous landed implementation:** `T030` is complete. The workspace request header now exposes request-level environment selection, new drafts seed from the current default environment, save/run block on missing saved environment references, and runtime execution/history now carry server-owned environment resolution metadata.
- **Latest planning refresh:** `T031` is complete. It confirms that `T030` does not automatically promote environment transfer, top-bar selector work, or any other parked follow-up, and that Candidate A's environment-transfer lane is now more concrete but still broader than one narrow task.
- **Latest environment lane narrowing:** `T032` is complete. It confirms that richer resolved-preview UX is the strongest future environment lane if one later task is needed, while top-bar selector work and environment transfer remain parked.
- **Latest resolved-preview narrowing:** `T033` is complete. Inside the richer resolved-preview area, post-run bounded resolution summary in the active result panel and persisted history is now the strongest future narrow lane, while pre-run unresolved-feedback tiering and explorer/readability copy remain parked.
- **Latest resolution-summary contract:** `T034` is complete. The future narrow lane is now fixed to one shared execution/history `EnvironmentResolutionSummary` contract rendered in `Execution Info`, not a new tab or full resolved-request inspector.
- **Active highest-priority next step:** no new implementation task is promoted automatically after `T052`. Future workspace follow-up should start from `../tasks/task-052-workspace-request-group-creation-and-save-placement-controls.md` and then narrow one concern such as request-group rename/delete UI, collection CRUD, environment/script bundle expansion, or result-panel tuning under detached-draft semantics instead of reopening another broad explorer/layout redesign. If a future verification step is sandbox-blocked, ask for a local rerun with exact commands and expected results instead of holding the backlog open for sandbox clearance.
- Future follow-up work in this area should start from `../tasks/task-030-request-environment-selection-and-runtime-resolution.md` and treat top-bar environment selection, richer resolved-preview UX, request-stage script attachment/reference semantics, settings mutation, and environment/script import-export expansion as separate deferred scope rather than as unfinished placeholder cleanup.
- `T028` remains the post-`T027` Candidate A refresh that moved environments and standalone saved scripts out of placeholder-only status, but `T031` is now the latest refresh after `T030` and it still does not promote environment/script authored-resource transfer.
- `T029` fixed the intended request-builder environment slice boundary, and `T030` is now the implemented baseline for that boundary.
- `T031` is now the latest post-`T030` no-promotion review: the new request/runtime environment coupling makes environment transfer more obviously real but still too broad to promote, and it does not make top-bar selector work baseline completion.
- `T032` is now the canonical lane comparison for future environment work: resolved-preview refinement wins over top-bar selector work and environment transfer as the strongest future lane, but no implementation is promoted yet.
- `T033` is now the canonical sub-lane comparison for future resolved-preview work: post-run bounded resolution summary wins over pre-run feedback tiering and explorer/readability copy as the strongest future narrow lane, but no implementation is promoted yet.
- Use `resolved-preview-sub-lane-comparison.md` after `environment-follow-up-lane-comparison.md` whenever future environment work starts from the resolved-preview lane so contributors do not reopen validation-tier work and copy polish as if they were the same slice.
- Use `../architecture/client-i18n-foundation.md`, `../tasks/task-037-client-i18n-foundation-and-korean-locale-bootstrap.md`, `../tasks/task-038-environments-and-scripts-route-localization-pass.md`, `../tasks/task-039-workspace-authoring-localization-pass.md`, `../tasks/task-040-workspace-result-panel-localization-pass.md`, `../tasks/task-041-captures-observation-route-localization-pass.md`, `../tasks/task-042-history-observation-route-localization-pass.md`, `../tasks/task-045-mocks-route-localization-pass.md`, and `../tasks/task-046-shell-and-management-generated-aria-localization-pass.md` before future localization work so later Korean translation passes start from the shared provider, locale storage key, bounded route-management translation, bounded workspace authoring translation patterns, bounded workspace observation-panel translation patterns, the landed full observation-route translation patterns, and the bounded shell/management/workspace-explorer generated-label pattern instead of from new one-off string rewrites.
- `T034` is now the canonical contract-definition note for that narrowed lane: future environment-observation implementation should start from `request-environment-resolution-summary-contract.md` so result/history scope stays bounded to one shared summary object.
- Use `environment-follow-up-lane-comparison.md` before proposing any new environment follow-up so deferred environment work starts from one compared lane rather than from a broad “finish environments” theme.
- Use `m3-f3-implementation-handoff.md` as the canonical applied-patch reference plus local-verification handoff note instead of reopening the wrapper/CSS scope audit.
- Use `../tasks/task-026-m3-f3-validation-environment-blocker-triage.md` before proposing any additional repo-side blocker fix so contributors start from the confirmed wrapper/preflight boundary instead of retrying already-landed mitigations.
- Use `../tasks/task-025-post-m3-f3-closure-priority-review.md` as the latest no-promotion decision record after `M3-F3` closure.
- No new post-M3 implementation task is promoted automatically in this pass; `T025` confirms that Candidate A remains the strongest parked area but its remaining `/mocks`-local import lane is still too ambiguous, while Candidate B and Candidate C also remain parked.
- T015, T018, T019, T020, T021, T022, T023, T024, T025, and T026 are now closed. Post-M3 priority review, Candidate A narrowing, Candidate B lane comparison, Candidate B import-migration approach decision, Candidate C gap inventory, the M3-F3 handoff, the post-M3-F3 closure review, and the environment-blocker triage delivered bounded authored-resource, compatibility, packaging, validation-continuity, and hold-state planning follow-ups, while later migration-engine implementation, Candidate 2, and bounded packaging polish remain parked rather than reopening shell scope or broad backlog themes.
- Candidate 2 remains parked after `T019` because the repo still treats import ownership as workspace-level mixed-bundle flow rather than a clearly local `/mocks` import seam, so route-local import symmetry would still reopen broader resource-tooling ambiguity.
- Use `post-m3-reactivation-guide.md` as the canonical rule set before requesting extra local confirmation of `M3-F3` or promoting any parked post-M3 backlog item.
- Use `candidate-a-promotion-readiness.md` before considering further Candidate A work beyond `T019` so authored-resource proposals are checked against the shipped request/mock transfer baseline instead of broad future-tooling language.
- Use `candidate-a-gap-inventory.md` before considering further Candidate A work beyond `T019` so authored-resource proposals start from a concrete current gap inventory rather than from speculation.
- Use `candidate-a-narrow-candidate-comparison.md` to understand why Candidate 2 stayed parked and why Candidate 1 won before `T019` was created.
- Use `candidate-b-promotion-readiness.md` before considering later migration-engine work so any future Candidate B proposal starts from the shipped classification baseline instead of a broad upgrade-platform idea.
- Use `candidate-b-gap-inventory.md` before considering later migration-engine work so any future Candidate B proposal starts from concrete lane evidence. The current stronger future lane is authored-resource import handling for `migration-needed` bundle/resource versions, but it remains parked.
- Use `candidate-b-narrow-lane-comparison.md` before considering later migration-engine work so Candidate B does not skip the runtime-versus-authored-resource lane comparison before any future implementation discussion.
- Use `candidate-b-import-migration-approach-decision.md` before considering authored-resource import migration work so the stronger Candidate B lane starts from one chosen bundle-level normalization framing instead of a broad migration solution space.
- Use `candidate-c-promotion-readiness.md` before considering packaging polish so Candidate C starts from the shipped `check:app` and `/api/app-shell-status` baseline instead of from the already known environment-bound esbuild limitation.
- Use `candidate-c-gap-inventory.md` before considering packaging polish so Candidate C starts from concrete shipped-gap evidence instead of from broad packaging language or environment noise.
- S1 is implemented with the new client bootstrap, route/provider scaffold, persistent shell regions, component coverage, and a conservative legacy coexistence seam.
- S2 is implemented with the `/workspace` explorer scaffold, in-memory request tree, request tab registry shell, tab focus/close behavior, active work-surface placeholder, and contextual result-panel placeholder.
- S3 is implemented with per-tab request draft state, method/url authoring, params/headers/body/auth core editors, scripts placeholder, dirty tab indicators, and a still-separated observation panel seam.
- S4 is implemented with a typed runtime-events adapter seam, synthetic normalized capture feed, shell-level connection health, and a `/captures` list/detail/timeline skeleton that keeps mock outcome vocabulary separate from authoring state.
- S5 is implemented with props-only shared result/detail primitives, a refactored request observation placeholder, and a refactored captures observation surface that reuse tabs, summary sections, empty callouts, and family-aware badges without changing feature state ownership.
- S6 is implemented with a feature-local `/history` observation store, synthetic execution history fixtures, shared-primitive-based result composition tabs, compact stage summaries, and explicit execution/transport/test outcome family separation that remains independent from captures and request drafts.
- S7 is implemented with a feature-local /mocks rule-management store, synthetic rule fixtures, a shared-primitive-based list/detail skeleton, local New Rule draft entrypoint, and explicit separation between authored rule state and runtime mock outcome vocabulary.
- S8 is implemented with an explicit replay bridge layer, capture/history-to-draft normalization, edit-first Open Replay Draft entrypoints, and new workspace replay tabs that hydrate authoring state without mutating observation records.
- S9 is implemented with stage-aware request-bound script drafts, a lazy-loaded Scripts editor surface for Pre-request/Post-response/Tests, per-tab script persistence, and explicit separation from execution/history/captures observation state.
- S10 is implemented with smoke/readiness copy refinement across workspace, captures, history, mocks, replay, and scripts, keeping authoring and observation surfaces legible without widening feature scope.
- S11 is implemented with actual request save/run wiring, storage-backed saved request refresh in the workspace explorer, active-tab run observation in the right-hand result panel, and continued separation between authoring draft state and observation result state.
- S12 is implemented with real /history query wiring against the SQLite runtime lane, persisted execution detail/result composition loading, run-to-history visibility through query invalidation, and continued separation between active-tab run state and history observation state.
- S13 is implemented with real /captures query wiring against persisted runtime capture records, query-driven capture list/detail loading, runtime-event-triggered refresh against canonical capture queries, and continued separation between captures observation state and request draft/history state.
- S14 is implemented with richer bounded diagnostics across the active request result panel, persisted history detail, and persisted captures detail, keeping family-aware badges and authoring/observation state separation intact.
- S15 is implemented with bounded pre-request/post-response/tests script execution in the run lane, structured/redacted stage summaries in the active request result panel, and persisted script diagnostics summaries in history without merging authoring state into runtime observation state.
- S16 is implemented with persisted mock-rule CRUD, real /mocks query-driven management UI, inbound capture evaluation against enabled priority-ordered rules, and capture outcome summaries that stay separate from authored mock-rule state.
- S17 is implemented with clarified request create-vs-update save semantics, replay draft save stabilization, persisted-first workspace explorer ordering, predictable mock rule ordering and selection updates, and bounded runtime request snapshot linkage metadata that keeps resource and runtime lanes distinct.
- S18 is implemented with captures/history snapshot fidelity refinement, clearer bounded-vs-redacted preview wording, tighter selection fallback after filter/refresh changes, and better cross-surface observation summary consistency without changing state ownership.
- S19 is implemented with wording and diagnostics polish across the request result panel, scripts authoring surface, captures, history, mocks, and replay cues, aligning bounded/redacted/deferred copy without changing semantics or state ownership.
- S20 is implemented with authored resource-lane import/export for saved request definitions and mock rules, a safe new-identity import policy, workspace explorer import/export entrypoints, and no runtime lane history/captures artifact transfer.
- S21 is implemented with explicit resource/runtime schema markers, deterministic resource manifest/serialization helpers, stricter authored-resource bundle validation, runtime metadata compatibility checks, and low-level storage seam tests without changing save/run/history/captures/mocks semantics.
- S22 is implemented with bounded repo/tooling cleanup: split lint/test scripts, CommonJS-aware ESLint overrides, normalized Node seam-test entrypoints, and a safer Vitest startup path that clarifies the remaining Windows sandbox `esbuild` limitation.
- S23 is implemented with single-resource authored bundle export for saved requests and mock rules, richer import accept/reject summaries, bounded imported-name previews, and continued exclusion of runtime history/captures/execution artifacts from transfer scope.
- S24 is implemented with explicit compatibility classification helpers, resource-manifest/runtime-metadata migration-needed vs unsupported-version handling, read-compatible legacy bundle/resource paths, and additional low-level seam tests without introducing a write-time migration engine.
- S25 is implemented with clearer dev/build/serve script taxonomy, a built-shell-aware `/app` fallback page, `/api/app-shell-status`, `/app` build-base alignment for the React shell, and a lightweight README/check script that make app packaging and local startup expectations easier to verify without changing feature semantics.
- S26 is implemented with a shared esbuild sandbox-compatibility preflight helper, fail-fast bounded wrapper messaging for `build:client` and `test:ui`, `check:app` compatibility reporting, and script-level Node seam coverage that narrows the remaining Windows/sandbox blocker without changing feature behavior.
- A Material 3 adoption plan now exists in `docs/architecture/material-3-adoption-plan.md`, and the initial token/theme rollout plus shell/shared-primitives/request-builder/observation-surface materialization, top-bar role legend, route-role cues, role-specific panel accents, role-specific tab/header treatments, feature-level list/detail/contextual role strips, and the `M3-F2` accessibility/contrast/focus/density polish pass now form the visual-system baseline for later incremental polish.

## Blockers Snapshot
- T010 shell slices S1-S26 are now landed. `T019`, `T020`, `T021`, `T022`, `T023`, `T024`, and `T026` are also landed.
- `T025` now confirms that closing `M3-F3` does not automatically justify a new implementation task. Candidate A remains the strongest parked area, but its remaining `/mocks`-local import lane still has ownership ambiguity, and Candidate B/C remain parked.
- `M3-F3` is treated as complete in tracking: the wrapper/CSS patch is in code, `npm.cmd run typecheck` passed, a user-verified non-sandbox `npm.cmd run test:ui` passed the then-current full UI suite on 2026-03-22, and same-day `npm.cmd run check` passed in this sandbox.
- `T026` confirms that the current repo already applies the main bounded mitigation work available at this layer: direct esbuild preflight classification, fail-fast Vite/Vitest wrappers, config-loader runner enforcement, Windows `net use` patching, and transformed-module gate probes.
- Future sandbox-only verification failures should be handled through local command handoff per `AGENTS.md` rather than as an active delivery blocker.
- `T027` is implemented with persisted environment CRUD, persisted standalone saved-script CRUD plus read-only templates, and a diagnostics-first `/settings` route. Those routes are no longer placeholder-only surfaces.
- Architecture, schema, safety, persistence, request-builder, script-UX, mock-rule, history/inspector, QA, tooling, and shell-slicing inputs are now documented.

