import { SectionPlaceholder } from '@client/features/shared-section-placeholder';

export function EnvironmentsPlaceholder() {
  return (
    <SectionPlaceholder
      title="Environments"
      summary="Environments section scaffold for the persistent shell."
      sidebarLabel="Environments sidebar and explorer content will land in a later slice."
      detailLabel="Environments contextual result and detail content will land in a later slice."
    />
  );
}
