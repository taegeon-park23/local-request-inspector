import { SectionPlaceholder } from '@client/features/shared-section-placeholder';

export function ScriptsPlaceholder() {
  return (
    <SectionPlaceholder
      title="Scripts"
      summary="Scripts section scaffold for the persistent shell."
      sidebarLabel="Scripts sidebar and explorer content will land in a later slice."
      detailLabel="Scripts contextual result and detail content will land in a later slice."
    />
  );
}
